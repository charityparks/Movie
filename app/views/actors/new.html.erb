<h2> New Actor </h2>

<%= render partial: '/errors', locals: { record: @actor} %>

<%= form_for @actor do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br><br>
    <%= f.submit "Create Actor" %>
<% end %>
